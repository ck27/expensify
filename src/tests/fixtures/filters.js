import moment from "moment";

const defaultFilters = {
    text: "",
    sortBy: "date",
    startDate: undefined,
    endDate: undefined
};

const altFilters = {
    text: "",
    sortBy: "date",
    startDate: undefined,
    endDate: undefined
};

export { defaultFilters, altFilters};
